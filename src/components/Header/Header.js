import React from 'react'
import styles from './Header.module.css'

const Header = () => {
    return ( 
        <div className={styles.mainHeader}>
            how we actually store variables
        </div>
    )
}
 
export default Header;